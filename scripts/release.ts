// TODO(phase-9): build all plugins, run validate:plugins, bump openclaw.compat ranges only after conformance passes,
// publish @clawos/* to npm and the gatekeeper catalog to ClawHub (`clawhub package publish`).
console.log("release: not implemented (Phase 9)");
