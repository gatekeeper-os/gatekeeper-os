{
  "name": "@clawos/cli",
  "version": "0.1.0",
  "description": "The clawos binary: install, cells, status, doctor, config apply, backups.",
  "type": "module",
  "bin": { "clawos": "./bin/clawos.js" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "bin", "templates", "README.md"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean && node scripts/stage-templates.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": { "tsup": "catalog:", "typescript": "catalog:", "vitest": "catalog:", "@types/node": "catalog:" }
}
