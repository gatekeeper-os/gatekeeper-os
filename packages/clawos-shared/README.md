# @clawos/shared

The contracts (plan §4.3). **Kernel bar**: every exported member is doc-commented; changes are reviewed line by line.
Depends only on `typebox`. Wire schemas for RPC payloads live in `src/schemas.ts`.
