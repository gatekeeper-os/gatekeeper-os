# Blueprint: ops

Scheduled operations agent: cron-driven summaries and notifications.

Applied with `clawos blueprint apply ops --agent <id>`. This README is added to the agent's bootstrap files by the `clawos-bootstrap` hook.
