# Blueprint: researcher

Web research agent with fetch/search and the generic HTTP gatekeeper.

Applied with `clawos blueprint apply researcher --agent <id>`. This README is added to the agent's bootstrap files by the `clawos-bootstrap` hook.
