# Blueprint: assistant

Messaging-only personal assistant. No filesystem or shell access; uses gatekeepers for everything external.

Applied with `clawos blueprint apply assistant --agent <id>`. This README is added to the agent's bootstrap files by the `clawos-bootstrap` hook.
