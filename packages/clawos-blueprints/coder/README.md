# Blueprint: coder

Sandboxed coding agent with scoped filesystem and exec inside a container.

Applied with `clawos blueprint apply coder --agent <id>`. This README is added to the agent's bootstrap files by the `clawos-bootstrap` hook.
