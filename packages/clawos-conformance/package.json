{
  "name": "@clawos/conformance",
  "version": "0.1.0",
  "description": "Conformance suite run against a live Gateway; defines 'compatible' for an upstream version.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "openclaw.plugin.json", "config.schema.json", "hooks", "README.md"],
  "scripts": { "build": "tsup src/index.ts --format esm --dts --clean", "test": "vitest run", "typecheck": "tsc -p tsconfig.json --noEmit" },
  "dependencies": { "@clawos/shared": "workspace:*" },
  "devDependencies": { "tsup": "catalog:", "typescript": "catalog:", "vitest": "catalog:", "@types/node": "catalog:", "tsx": "catalog:" }
}
