{
  "inputs": [
    { "id": "GITHUB_OAUTH_CLIENT_ID", "kind": "string", "title": "GitHub OAuth App client ID" },
    { "id": "GITHUB_OAUTH_CLIENT_SECRET", "kind": "secret", "title": "GitHub OAuth App client secret" }
  ],
  "consoleUrl": "https://github.com/settings/developers",
  "setupSteps": [
    "Create an OAuth App (or use Device Flow with a client ID only).",
    "Set the callback URL to the redirect URI below.",
    "Copy the client ID and secret into `gkos gatekeeper add github`."
  ],
  "redirectUriTemplate": "{PUBLIC_BASE_URL}/os/gatekeeper/github/oauth/callback"
}
